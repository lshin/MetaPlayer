

Questions
    What is MetaQ?
        - timed events
        - inventory management / fulfilment

    What does a metaq config look like?
        - start / [end || duration || null]
        - type
        - type properties (key/val)

    What metaq types do we want to support?
        - iframe / html embed
        - advertisement / dart?

Player:

    // interface
    - pause
        html5.pause()
    - play
        html5.play()
    - seek
        html5.currentTime = N
    - time
        html5.currentTime

    // events
    - event.loaded
        html5.event.loadstart

    - event.play
        html5.event.playing
        html5.event.play

    - event.paused
        html5.event.paused

    - event.seek
        html5.event.seeking
        html5.event.seeked

    - event.ended
        html5.event.ended

    - event.volumechange
        html5.event.volumechange


    // optional
    - buffers

    // playlist
    - current index
      custom.index

    - track select
      custom.index = N

    - event.trackChange
        event.custom.trackChange

    - playlist add
    - playlist delete
    - playlist empty


